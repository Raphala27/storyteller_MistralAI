# MistralAI API Configuration
MISTRAL_API_KEY=your_mistral_api_key_here

# PostgreSQL Database Configuration
# For local development:
# DATABASE_URL=postgresql://localhost/storyteller_db
# Or with credentials:
# DATABASE_URL=postgresql://username:password@localhost/storyteller_db

# For Render.com (will be auto-provided):
# DATABASE_URL=postgres://user:pass@host/dbname
# Note: The app will automatically convert postgres:// to postgresql://

# Example production URL format:
# DATABASE_URL=postgresql://user:password@host:5432/database_name

# JWT Authentication Configuration
# Generate a secure random secret key (use: openssl rand -hex 32)
JWT_SECRET_KEY=your_secret_key_here_generate_with_openssl_rand_hex_32

# Redis Configuration for Rate Limiting
# For local development:
REDIS_URL=redis://localhost:6379/0

# For cloud Redis (Upstash, Redis Labs, Render Redis, etc.):
# REDIS_URL=redis://default:password@host:port
# Or with TLS:
# REDIS_URL=rediss://default:password@host:port

# Note: Rate limiting will be automatically disabled if Redis is unavailable
# This allows the app to work without Redis in development
